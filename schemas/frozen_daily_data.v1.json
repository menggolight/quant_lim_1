{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://quant.local/schemas/frozen_daily_data.v1.json",
  "title": "Frozen daily controlled-data envelope v1",
  "description": "After-close updater receipt. Hashes prove content consistency only; controlled external adapters remain responsible for source authentication.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "update_id",
    "alpha_snapshot_sha256",
    "non_alpha_exposure_metrics",
    "instrument_rules",
    "source_authentication",
    "data_update_sha256"
  ],
  "properties": {
    "schema_version": {"const": "frozen-daily-data.v1"},
    "update_id": {"type": "string", "minLength": 1, "pattern": "^\\S+$"},
    "alpha_snapshot_sha256": {"$ref": "#/$defs/sha256"},
    "non_alpha_exposure_metrics": {
      "type": "array",
      "minItems": 5,
      "maxItems": 5,
      "items": {"$ref": "#/$defs/metric"}
    },
    "instrument_rules": {
      "type": "array",
      "minItems": 1,
      "items": {"$ref": "#/$defs/instrumentRule"}
    },
    "source_authentication": {
      "const": "external_controlled_adapters_required_hash_is_not_source_proof"
    },
    "data_update_sha256": {"$ref": "#/$defs/sha256"}
  },
  "$defs": {
    "sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "date": {"type": ["string", "null"], "format": "date"},
    "dateTime": {"type": ["string", "null"], "format": "date-time"},
    "metric": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "category",
        "status",
        "value",
        "observation_session",
        "available_at",
        "source_snapshot_sha256",
        "failure_codes"
      ],
      "properties": {
        "category": {
          "enum": [
            "CSI800_TOTAL_RETURN_TREND",
            "MARKET_BREADTH",
            "REALIZED_VOLATILITY",
            "MARKET_DRAWDOWN",
            "ACCOUNT_DRAWDOWN"
          ]
        },
        "status": {"enum": ["OK", "DATA_FAILED"]},
        "value": {"type": ["number", "null"]},
        "observation_session": {"$ref": "#/$defs/date"},
        "available_at": {"$ref": "#/$defs/dateTime"},
        "source_snapshot_sha256": {
          "oneOf": [{"$ref": "#/$defs/sha256"}, {"type": "null"}]
        },
        "failure_codes": {
          "type": "array",
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1}
        }
      }
    },
    "instrumentRule": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "instrument_id",
        "name",
        "instrument_type",
        "lot_size",
        "tick_size",
        "sell_stamp_duty_rate",
        "t_plus_one"
      ],
      "properties": {
        "instrument_id": {"type": "string", "pattern": "^[0-9A-Z][0-9A-Z.]{2,31}$"},
        "name": {"type": "string"},
        "instrument_type": {"type": "string", "minLength": 1},
        "lot_size": {"type": "integer", "minimum": 1},
        "tick_size": {"type": "string", "pattern": "^(?:0\\.(?:0*[1-9][0-9]*)|[1-9][0-9]*(?:\\.[0-9]+)?)$"},
        "sell_stamp_duty_rate": {"type": "string", "pattern": "^(?:0|0\\.[0-9]+)$"},
        "t_plus_one": {"type": "boolean"}
      }
    }
  }
}
