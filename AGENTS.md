# AGENTS.md

本文件适用于整个仓库，是本项目唯一的 Agent 与协作者规则源。

## 核心方法

牢记第一性原理与对抗式审查。

- 先定义要预测或改变的对象、可执行时点、真值、基准、期限和失败条件，再写代码或解释结果。
- 主动寻找能推翻当前结论的证据；把数据可用误差、样本选择、幸存者偏差、未来函数、重复观点、交易成本和行业集中暴露当作默认威胁。
- 区分“实现完成”“测试通过”“数据已接入”“统计有效”“允许模拟”和“允许实盘”。它们不是同一状态，禁止跨级表述。
- 不以复杂度代替证据。低自由度、可解释、可复现的基线优先于未经样本外验证的复杂模型。

## 真源与时点

发生冲突时，按以下顺序处理：

1. 用户本轮明确要求与最新附件；
2. 版本化 Schema、配置和正式规格；
3. 标准 CLI 的受控产物与运行清单；
4. 测试和样例；
5. README、计划和历史说明。

任何研究数据都必须尽可能记录：`source`、`subject_id`、`observed_at`、`available_at`、版本、口径和内容哈希。只有在决策时点已经可获得的数据才能进入该时点的特征。宏观数据使用首次公布值；财报使用首次披露值；后续修订仅作附表或显式独立特征。

日期只有自然日的研报，只有在受控交易日历确认后，才可以“下一交易日开盘”作为正式执行时点。调用者自行传入的布尔认证、来源名称或哈希不等同于官方来源证明。

## 不可突破的安全边界

- 当前交易状态固定为 `paper_only`；不得新增、启用或伪装任何实盘下单、撤单或自动授权能力。
- 华泰 MQuant 仅允许只读 Shadow。未完成官方客户端授权、当前 SDK 契约核验、账户人工核对和来源认证前，必须保持 `blocked`。
- 研报审计当前为 `research_only_not_trade_eligible`。官方真值、交易日历、客观因子和时点行业映射没有通过受控适配器准入前，正式基本面榜和 M1 必须保持 `diagnostic/not_admitted`。
- 只信任标准 CLI 生成并通过 manifest、Schema、版本和完整性检查的正式产物。直接写数据库、调用内部存储 API 或手工构造模型对象只能用于诊断和测试。
- 禁止将密码、验证码、Token、Cookie、资金账号、股东账号、身份证号、手机号或绑定秘密写入仓库、日志、测试夹具和聊天内容。
- 文件 SHA-256 只能证明内容一致性，不能证明文件来自券商、监管机构或其他官方来源。

若用户未来要求扩大权限，先实现独立、显式、可审计的阶段门禁；不得通过修改一个配置布尔值绕过风险边界。

## 目录职责与依赖方向

- `agent/`：采集、规范化和日常编排。不得绕过外部权限，不承载交易决策。
- `research/`：研究算法、研报审计、来源技能、因子和统计评价。不得直接调用券商下单接口。
- `trading/`：显式信号桥、风险门禁、Paper 账本和只读券商适配。只消费已定义契约的结构化输入。
- `integrations/`：在官方或外部运行环境中使用的边界脚本。外部 SDK 对象必须先规范化，不能渗透到核心领域模型。
- `configs/`：版本化策略与研究政策，不存运行秘密或个人账户标识。
- `schemas/`：跨进程或跨系统的数据契约。破坏性变更必须升级 Schema 版本并保留迁移或明确拒绝旧版。
- `data/`：输入证据和生成产物，不放业务逻辑。样例以 `*.sample.*` 命名，个人数据默认不提交。
- `docs/`：规格、计划、架构和接入证据。计划不能覆盖代码、配置和真实运行结果。
- `tests/`：正常、负向、边界、时点、版本和对抗性验证。

允许的主要依赖方向是 `agent/integrations -> 规范化契约 -> research/data -> trading 的显式桥`。禁止 `research -> 券商写接口`，禁止展示层反向修改原始审计数据，禁止把完整券商账户持仓自动认领为策略资产。

## 修改契约

### 研报审计

- 保持宏观、行业、个股三张原始准确率表独立；综合因子不得反向更新它们。
- 当前正式输出文件名固定为配置中的 11 项。增删或改名必须同步配置、报告生成、manifest、离线确定性校验、README 和测试。
- 修改抽取规则时必须升级抽取器版本；Prompt 版本、PDF SHA-256 与抽取器版本共同参与缓存键。
- 修改 `models.py`、`storage.py`、Schema 或 claim/outcome 语义时，必须处理版本迁移、规范 ID、历史快照隔离和旧数据拒绝策略；`claim_id` 必须绑定完整抽取语义，同 ID 不同载荷必须拒绝而非覆盖。
- 只有已经到期且真值在评价时点可用的预测才能更新来源技能；当前报告不能使用自己的未来结果给自己加权。
- 新增因子或交互项必须预注册。V1 只允许宏观×行业和行业×个股，不得静默加入三阶交互。
- 个股绝对评级与绝对 EPS 只用于审计；正式 `stock_report_factor` 只能使用明确的评级变化、目标价空间和盈利/EPS 修正。缺失值不得补成 0，“维持买入”不得解释为“评级上调”。
- 当前大文件的拆分会改变源码哈希、manifest 和大量私有函数测试绑定；除非先设计迁移并更新验证证据，不做纯审美式重构。

### 行业雷达与交易

- 行业分类、成分、基准和特征必须采用决策时点版本；当前 R0 只允许标为启发式基线。
- `trading/strategy_bridge.py` 是研究信号到执行层的唯一显式入口之一，不得让 Markdown、原始研报、行业雷达排名或合成数据直接形成订单。
- 风险检查必须 fail-closed：字段缺失、行情过期、快照不完整、账户绑定不符、来源未认证或状态未知时一律拒绝。
- 用户长期持仓和未显式划入的资产不能被策略账本认领。

## 工作流程

1. 先阅读根 README、目标目录 README、相关配置、Schema、规格和现有测试。
2. 检查工作区现有改动；用户文件和无关改动不得覆盖、清理或回滚。
3. 写出最小变更面和可证伪验收条件。能通过新增适配器解决的问题，不侵入核心模型。
4. 实现最小、可逆补丁；避免无关重命名、全仓格式化和为了“整洁”移动稳定路径。
5. 先运行最相关测试，再运行完整测试。涉及研报产物时还要验证离线双跑哈希一致。
6. 对照失败模式做一次对抗式复核：尝试伪造来源、未来时间、重复报告、缺字段、旧 Schema、不完整快照和绕过 CLI。
7. 交付时分别报告：完成内容、真实测试证据、未覆盖项、仍阻塞项和安全状态。不得把预期行为写成已验证事实。

## 测试基线

完整回归：

```powershell
python -m unittest discover -s tests -v
```

研报审计专项：

```powershell
python -m unittest discover -s tests -p "test_broker_report_audit*.py" -v
```

仅文档或忽略规则变更，也至少要检查链接、命令、文件路径和状态描述是否与当前代码及配置一致。测试产生的临时数据库、缓存和报告应进入临时目录，不能覆盖样例或真实研究产物。

## 文件与提交纪律

- 使用 `rg` / `rg --files` 搜索；修改文件优先使用小范围补丁。
- 不删除或移动 `data/portfolio/`、非 sample 报告、Obsidian 内容和其他用户数据，除非用户明确授权并已核对目标路径。
- `data/tmp/`、根 `.tmp/`、缓存、SQLite、`__pycache__` 和本地运行报告是生成物；可以通过忽略规则隔离，但不要擅自批量删除。
- `.gitignore` 不能清除已经跟踪的秘密；发现疑似敏感信息时停止扩散并明确报告。
- 文档使用中文为主，变量、命令、状态码和 Schema 名保留源码拼写。

## 完成定义

任务只有在以下条件同时满足时才算完成：

- 用户要求的结果已经实际生成或修改；
- 行为、配置、Schema、文档和测试彼此一致；
- 有与风险相称的真实运行证据；
- 时点、来源、失败模式和隐私边界已检查；
- 尚未完成或尚未获权的能力被清楚标为阻塞，而不是用乐观措辞掩盖。
